import type { FC, JSX } from 'react';

/**
 * Component to show a server by id.
 *
 * @returns { JSX.Element } Component to show a server by id.
 */
const ServerIdPage: FC = (): JSX.Element => {
    return (
        <>
            <h1>ServerId</h1>
        </>
    );
};

export default ServerIdPage;
