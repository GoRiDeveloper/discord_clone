/**
 * Http code errors.
 */
export enum HTTP_CODE_ERRORS {
    INTERNAL_ERROR = 500,
    BAD_REQUEST = 400,
    UNAUTHORIZED = 401,
    NOT_FOUND = 404,
    METHOD_NOT_ALLOWED = 405,
}

/**
 * Http codes.
 */
export enum HTTP_CODE {
    SUCCESS = 200,
}
